<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <h1>'<%= user_id %>'님의 예약 조회 결과</h1>
        <a href="/">처음으로</a><br><br>
        <table border = '3'>
            <th>캠핑장 이름</th>
            <th>등급</th>
            <th>날짜</th>
            <th>취소하기</th>
        <% for(let i = 0; i < result.length; i++){%>
            <tr>
                <td><%= result[i].name %></td>
                <td><%= result[i].grade %></td>
                <td><%= result[i].date %></td>
                <td>
                    <form action="/reservation/cancel" method="get">
                        <input type="hidden" value=<%= result[i].r_id %> name="r_id">
                        <input type="submit" value="GO">
                    </form>
                </td>
            </tr>
        <%}%>
    </body>
</html>
