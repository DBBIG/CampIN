<!DOCTYPE html>
<html>
    <head>
        <title>DBbig</title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <h1>'<%= keyword %>' 등급 검색 결과</h1>
        <a href="/">처음으로</a><br><br>
        <table border = '3'>
            <th>캠핑장 이름</th>
            <th>주소</th>
            <th>자세히 보기</th>
        <% for(let i = 0; i < result.length; i++){%>
            <tr>
                <td><% result[i].name %></td>
                <td><% result[i].address %></td>
                <td>
                    <form action="/search/state" method="get">
                        <input type="hidden" value=<%= result[i].cp_id %> name="cp_id">
                        <input type="submit" value="GO">
                    </form>
                </td>
            </tr>
        <%}%>
    </body>
</html>
